# GPIO28 unloaded scope check — 2026-09-11 20:51–20:54 JST

- UNO Q USB 2341:0078, ADB serial 2261748543, kernel 6.16.7-g0dd6551ae96b.
- Base commit 0f91aa7 plus local GPIO scope profile. libgpiod 2.2.1.
- gpiochip1 is 500000.pinctrl; line 28 initially input, mux/GPIO unclaimed.
- User connected CH1 to carrier marking 28; work proceeded under the prior
  condition that SD is disconnected. No audio playback/capture or servo motion.
- OWON VDS1022I25070056, hardware V5.0.1 / FPGA5 / API 1.1.5.
  CH1 x10/DC, range 50 V (API scale), rising threshold 0.9 V; CH2 not evaluated.
- Two captures: 10 kS/s then 1 MS/s. Both armed with already_triggered=0.
  After arming, SSH as airpocket invoked bounded gpioset on GPIO28:
  initial Low, toggle after 100 ms, after 50 ms, then exit after 100 ms.
  Pull-down requested; outer timeout 2 seconds. Command completed without error.
- Neither capture triggered; deadline produced StopIteration. No waveform saved.
  Both reports returned stop_acknowledged=true. Second timeout exposed a race
  between the API stop timer and USB disposal; wrapper now joins the timer before
  disposal (fix not yet verified with another physical timeout).
- Final gpioget --as-is returned 0; gpioinfo reported output and no consumer.
  No gpioset process remained. Released GPIO state is not a shutdown guarantee.
- Result: software request succeeded; physical High/Low NOT confirmed. Do not
  connect amplifier SD or interpret this as proof of a damaged/nonworking GPIO.
  Next verify physical carrier contact/ground/probe against SOC_GPIO_28, and
  independently check scope trigger/voltage behavior before further stimulus.
- Local evidence: local-runs/gpio28-20260911-01 and -02 (ignored by Git).

## After user restarted the scope (20:55 JST)

User reconfirmed GPIO28 and GND wiring. Attempted forced static acquisition,
but API refused because the scope FPGA is no longer initialized after power reset.
Official application initialization is required; no FPGA image was uploaded by
our wrapper. No waveform was acquired and no new High stimulus was issued.
The attempted directory gpio28-static-low-01 is NOT a measured Low record:
initial readback was 1, demonstrating released state cannot be relied upon.
A first cleanup using toggle periods 100ms,0 returned 1, not the intended Low.
Do not use toggle/exit semantics as a Low cleanup. Replaced with a constant
28=0 request held by gpioset and terminated by a 0.3-second timeout; readback 0.
SD remains disconnected under the test conditions. Static force option added;
physical verification pending official-app initialization.

## Official application initialization (20:57 JST)

Used Windows app automation to launch existing OWON VDS_C2, observed Ready,
then closed it. Forced static acquisition gpio28-static-low-02 succeeded:
5000 samples at 1 MS/s, API CH1 p10=0 V, p90=0.2 V, stop acknowledged.
Absolute voltage remains uncalibrated. A subsequent bounded 6-second High request
followed by constant Low cleanup completed, final GPIO readback 0. The subsequent
gpio28-static-high-01 acquisition did not immediately complete; host invocation
latency means the High interval was not proven to overlap capture. Do not use
that run to judge GPIO High amplitude or treat the pair as a verified toggle.
Official application confirmed closed. No amplifier connection or audio test.
High-named acquisition ultimately timed out with StopIteration and
stop_acknowledged=true; no High waveform was saved.

## AUTO static acquisition succeeds (21:03–21:04 JST)

API source inspection showed ONCE acquisition polls GET_DATAFINISHED and
GET_TRIGGERED, whereas AUTO can obtain static levels without satisfying an edge.
Changed the GPIO-only --force-static option to AUTO sweep (no force command).
This is a workaround for static measurements, not a diagnosed repair of ONCE.

| Run | API range | GPIO request | CH1 p10–p90 (uncalibrated API volts) |
|---|---|---|---|
| gpio28-auto-low-01 | 50 V | Low held 6 seconds | -0.2 to 0.2 |
| gpio28-auto-high-01 | 50 V | High held 6 seconds | 1.0 to 1.2 |
| gpio28-auto-low-02 | 50 V | After constant Low cleanup | -0.2 to 0.2 |
| gpio28-auto-high-range20-01 | 20 V | High held 6 seconds | 2.16 to 2.24 |

All captured 5000 samples at 1 MS/s, x10/DC, stop acknowledged. ADB stimulus
and host acquisition launched concurrently, with 0.8–1.2-second host delay before
scope launch; captures completed in about 0.7 seconds within the 6-second hold.
Board and host wall clocks are not synchronized; their timestamps must not be
used directly to calculate transport latency. No audio or reset was invoked.
High commands always followed by a constant Low request (not toggle cleanup).
Final GPIO readback 0, no persistent gpioset process.

Conclusion: CH1 changes with GPIO28 Low/High and returns to baseline. Physical
static level control is observed, but 1.8 V compliance is NOT established.
The same High yields about 1.1 V vs 2.2 V across ranges, reproducing the earlier
factor-of-two scope range inconsistency. Do not infer actual GPIO undervoltage
or select SD resistors from these API readings. Need known-voltage calibration
or an independent DC measurement before connecting SD. No startup/reset-safe
SD circuit has been implemented; GPIO28 remains only a tested candidate.

## SD node wired, shutdown-state gate (21:49–21:50 JST)

User reported microphone and amplifier connected and SD pulled down and joined
to GPIO28. Speaker connection status was not stated. No aplay/arecord process;
ALSA card present. Initial GPIO read 0. No audio or servo operation.

- Settled CH1, 20 V full range: p10=0.00 V, p90=0.08 V (insufficient resolution
  to distinguish the MAX98357A minimum B0 trip point of 0.08 V).
- Settled CH1, 10 V full range: p10=0.04 V, p90=0.08 V.
- GPIO28 then held as input, bias disabled, for six seconds. Digital read=0.
  Concurrent settled CH1 at 10 V range remained p10=0.04, p90=0.08 V.
- Repeated high-Z input at 5 V full range with zero-centered display:
  p10=0.040 V, p90=0.060 V, min=0.020 V, max=0.060 V.

All scope runs used x10/DC, 1 MS/s, 5000 samples, discarded six initial AUTO
frames, and acknowledged stop. API absolute-voltage calibration remains
uncertified, but the narrowest-range result and digital Low support the
shutdown-state gate. GPIO was left input with no gpiod consumer. No High was
issued after attaching the amplifier; loaded High verification awaits explicit
confirmation that the speaker is disconnected.

## Amplifier-loaded High and driver control (21:52–22:10 JST)

The user explicitly confirmed that the speaker was disconnected. The amplifier,
microphone, external 10 kOhm SD-to-GND pulldown and GPIO28-to-SD connection
remained present. No audible output was possible and all PCM used below was zero.

- Bounded GPIO28 High under amplifier load: CH1 p10=1.78 V, p90=1.80 V,
  min=1.76 V, max=1.82 V at the 5 V full-range API setting. The command then
  issued constant Low cleanup; final digital readback was 0.
- Settled Low confirmation after cleanup: CH1 min=0 V, max=0.04 V,
  p10=0 V, p90=0.02 V.
- The SD overlay DTB SHA256 was
  `6c2b0982545f9005bd85acd85a154dd6a2503ab976c6dd4e9e3d7a4882cef7cc`.
  It was installed under a distinct `/boot/efi/ichiping/` filename; neither the
  stock DTB nor the prior MI2S0 DTB was overwritten.
- After reboot, the running DT contained `sdmode-gpios` and `sdmode-delay` in
  `/proc/device-tree/ichiping-max98357a`; `gpioinfo` showed gpiochip1 line 28 as
  output with `consumer=sdmode`.
- A 250 kS/s CH1=SD, CH2=GPIO99/WS rising-edge capture measured SD Low near
  0.02 V and High near 1.82 V. First sampled WS activity preceded SD High by
  about 0.484 ms. This is external clock-to-enable timing, not the duration of
  the kernel's `mdelay(8)` from its trigger callback.
- A synchronized 2.5 kS/s continuous acquisition captured the complete bounded
  cycle across two adjacent frames. Concatenating the frames gives 1,289 High
  samples, approximately 515.6 ms. SD and the final sampled WS activity returned
  Low in the same 0.4 ms sample interval. WS is aliased at this acquisition rate;
  only presence and cessation, not frequency or sub-sample ordering, is claimed.

The first low-rate falling-edge attempt immediately consumed a stale transition,
and a later low-rate single-trigger attempt missed the pulse. Those are scope
acquisition failures and are not used as electrical evidence. Continuous AUTO
frames were retained in the final run so the result does not depend on that
single-trigger behavior. OWON stop was acknowledged on successful captures.

Verdict: the external pulldown provides a measured shutdown Low, GPIO28 supplies
a valid loaded High, the Linux driver owns the line Low while idle, enables only
during an active PCM transfer, and returns Low on the bounded reset/stop path.
This closes the SD electrical-control gate. It does **not** establish acceptable
speaker sound; the speaker remained disconnected throughout.

Final post-reset check at 22:16 JST again found the running DT property,
`consumer=sdmode`, no aplay/arecord process, and CH1 min=0 V, max=0.04 V,
p10=0 V, p90=0.02 V. The board was left powered with SD idle Low.
